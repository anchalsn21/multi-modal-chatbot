# ── Deep Learning ──────────────────────────────────────────────────────────
torch>=2.0.0
torchvision>=0.15.0
transformers>=4.35.0

# ── Data & ML ──────────────────────────────────────────────────────────────
scikit-learn>=1.3.0
pandas>=2.0.0
numpy>=1.24.0

# ── Web API ────────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9   # Required for UploadFile (voice/image endpoints)

# ── Phase 2: Voice ASR ─────────────────────────────────────────────────────
faster-whisper>=1.0.0     # CTranslate2-based Whisper — fast CPU inference
jiwer>=3.0.0              # Word Error Rate metric for evaluation

# ── Phase 3: Image retrieval ────────────────────────────────────────────────
faiss-cpu>=1.7.4          # FAISS similarity search (CPU build)
Pillow>=10.0.0            # Image decoding (JPEG, PNG, WebP, etc.)
opencv-python>=4.8.0      # OpenCV for image processing and display
librosa>=0.10.0           # Audio feature extraction (MFCCs, spectrograms)
matplotlib>=3.7.0         # Plotting for data exploration notebook
nltk>=3.8.0               # Stemming and lemmatisation (Porter + WordNet)

# ── Phase 4: Entity matching + session memory ────────────────────────────────
rapidfuzz>=3.0.0          # Levenshtein fuzzy matching for entity extraction (CPU, ~200KB)
cachetools>=5.0.0         # TTLCache utility (optional — used by context pruning)

# ── Voice query generation ────────────────────────────────────────────────────
pyttsx3>=2.90             # TTS for synthetic voice query generation

# ── LLM (Groq) ────────────────────────────────────────────────────────────────
groq>=0.4.0               # Groq Python SDK — LLaMA 3 inference for response generation
python-dotenv>=1.0.0      # Load .env secrets into os.environ at startup
